# Sanity CMS
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_TOKEN=your_sanity_token

# Revalidation webhook secret
REVALIDATE_SECRET=your_random_secret_string
NEXT_PUBLIC_SITE_URL=https://nurreport.name.ng

# Sanity CMS
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_TOKEN=your_sanity_token

# Revalidation webhook secret
REVALIDATE_SECRET=your_random_secret_string
NEXT_PUBLIC_SITE_URL=https://nurreport.name.ng

# Push notifications (run `npx web-push generate-vapid-keys` for the pair below)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key
VAPID_SUBJECT=mailto:news@nurreport.name.ng
PUSH_WEBHOOK_SECRET=your_random_secret_string

# Optional — production subscriber storage (falls back to a local JSON file otherwise)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

#Election Update
ELECTION_SECRET_TOKEN=tas_osun_secret_election_2026